extends layout
block content
  .w3-card-4
    header.w3-container.w3-teal
      h1 Acordãos Portugueses
    .w3-container
      table.w3-table-all
        tr
          th ID
          th Processo
          th Data do Acordão
          th Relator
          th Descritores
            
        each arquivo in processos    
          each processo in arquivo 
            tr
              td #[a(href="/"+processo._id)= processo._id]
              td= processo.Processo
              td= processo['Data do Acordão']
              td= processo.Relator
              td= processo.Descritores
              if admin
                  td
                      button.w3-btn.w3-round.w3-indigo.w3-margin-right  
                          a(href="/retrieveList/" + processo._id) Edit
                      button.w3-btn.w3-round.w3-indigo.w3-margin-right  
                          a(href="#") Delete
              
    footer.w3-container.w3-teal
      h5 Generated by shopList::EngWeb2023 in #{d}